# Landing page videos

Place raw phone footage here (no color grading), then set `videos.items[].src` in `data/pump_panel_landing/it.yaml`:

- `landing-unboxing.mp4` — Unboxing e Cablaggio Interno
- `landing-dry-run-test.mp4` — Test di simulazione marcia a secco

Example:

```yaml
src: /videos/landing/landing-unboxing.mp4
```
